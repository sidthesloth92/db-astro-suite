/** localStorage keys for the client-side Celestory session + identity. */

/** Published-profile session: handle + one-time delete token. */
export const SESSION_KEY = 'celestory.session.v1';

/** Editable viewer identity (display name + handle), shared across the app. */
export const IDENTITY_KEY = 'celestory.identity.v1';
