/**
 * The five steps shown in the how-to overlay, in order.
 *
 * Kept as data rather than markup so the overlay renders one numbered row per
 * entry and the copy has a single home.
 */
export const HOW_TO_STEPS: readonly string[] = [
  'Drop or pick a JPEG or PNG. It never leaves your browser.',
  'Stars are found for you straight away. Choose a preset, then shape them with the sliders — Length and Brightness drive the spikes, Diffusion adds a bloom.',
  'Click a star to add or remove its spikes. Double-click one to tune it on its own. Missed a star? Use the crosshair tool on the canvas to place one anywhere.',
  'Drag a star to re-centre its detection. Scroll to zoom, drag empty sky to pan.',
  'Drag the divider to compare before and after, then export at the original resolution.',
];

