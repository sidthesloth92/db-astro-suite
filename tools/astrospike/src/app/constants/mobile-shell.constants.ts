/**
 * Constants for the mobile shell (full-bleed stage plus a floating controls
 * sheet).
 */

/**
 * Selector matched against a pointerdown target to detect a drag on one of the
 * sheet's sliders. While that is happening the sheet fades so the canvas
 * underneath stays visible.
 */
export const SLIDER_TARGET_SELECTOR = 'dba-ui-micro-slider';
