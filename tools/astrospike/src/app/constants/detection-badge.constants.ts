/**
 * How long the detection badge states its count before clearing, in ms.
 *
 * Long enough to read on a slow glance, short enough that it is gone before the
 * user starts working the sliders — where the same figure lives permanently as
 * the star magnitude readout's total.
 */
export const DETECTION_BADGE_LINGER_MS = 2600;
