/**
 * URL of the bundled sample astrophoto (the Pleiades) loaded on startup so a
 * first-time visitor sees spikes on real stars before choosing a file.
 */
export const SAMPLE_IMAGE_URL = 'assets/img/pleiades-sample.jpg';

/**
 * File name the sample is loaded under — it flows into the export filename,
 * so it reads like a real source image rather than a blob.
 */
export const SAMPLE_IMAGE_FILE_NAME = 'pleiades-sample.jpg';
