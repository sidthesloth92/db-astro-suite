/**
 * URL of the bundled sample astrophoto (the Pleiades, landscape) loaded on
 * startup so a first-time visitor sees spikes on real stars before choosing a
 * file.
 */
export const SAMPLE_IMAGE_URL = 'assets/img/pleiades-sample.jpg';

/**
 * File name the sample is loaded under — it flows into the export filename,
 * so it reads like a real source image rather than a blob.
 */
export const SAMPLE_IMAGE_FILE_NAME = 'pleiades-sample.jpg';

/**
 * URL of the portrait sample (the Cygnus Wall) used below the mobile
 * breakpoint, where a landscape frame would leave most of a phone screen
 * empty.
 */
export const SAMPLE_IMAGE_MOBILE_URL = 'assets/img/cygnus-sample.jpg';

/** File name the portrait sample is loaded under. */
export const SAMPLE_IMAGE_MOBILE_FILE_NAME = 'cygnus-sample.jpg';
