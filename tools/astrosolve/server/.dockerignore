node_modules
npm-debug.log
.env
.DS_Store
.git
.gitignore
*.md

# Data directories — all runtime data is mounted from the host volume, never
# baked into the image (the catalog is rebuilt off-box via `npm run init-db`).
data/astrometry/
data/local-catalog/
data/uploads/

# Build/Orchestration scripts (run on host, not in container)
scripts/data/
scripts/deploy/

