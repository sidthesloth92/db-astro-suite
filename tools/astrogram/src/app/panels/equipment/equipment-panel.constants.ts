/**
 * Placeholder icons applied to a newly-added row so it never renders blank in
 * the card. Distinct per type; the user can still rename the row's label.
 */
export const DEFAULT_EQUIPMENT_ICON = '🔧';

/** Placeholder icon for a newly-added software row. */
export const DEFAULT_SOFTWARE_ICON = '💿';
