/**
 * Astrogram-local colour constants for data-bearing values that are not
 * theme tokens (e.g. the default colour a user-added "Custom" filter
 * starts with — picked because it's visually distinct from the built-in
 * filter palette).
 */

/** Default swatch for a freshly-added custom filter row. */
export const ASTROGRAM_CUSTOM_FILTER_COLOR = '#ff00ff';
