/**
 * Stable identifiers for the Astrogram top-bar segmented tabs. Maps 1:1 to
 * `CardDataService.activeMode` via `AstrogramTopBarComponent.onTabChange`.
 */
export type AstrogramTabId = 'infographic' | 'stellar';
