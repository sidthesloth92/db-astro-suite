/** One stop of the Ringed Planet body gradient. */
export interface RingedPlanetGradientStop {
  /** Stop offset along the gradient, e.g. `42%`. */
  readonly offset: string;
  /** Stop colour as a hex value. */
  readonly color: string;
}
